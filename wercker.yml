box: wercker/default
build:
  steps:
    -  script:
        name: Let's check the environment
        code: |-
          sudo apt-get install php5
          which php
          sudo ls -l /etc/init.d
