box: wercker/default
build:
  steps:
    -  script:
        name: Let's check the environment
        code: |-
          sudo add-apt-repository -y ppa:ondrej/php5
          wget http://localhost | true
          sudo apt-get update
          sudo apt-get install -f php5
          php5 -v
          which php

          # wget http://wordpress.org/latest.tar.gz


#          cat /etc/*-release
#          cat /etc/apt/sources.list
#          sudo echo "deb http://us.archive.ubuntu.com/ubuntu/ precise-backports main restricted universe multiverse" >> /etc/apt/sources.list
#          sudo apt-get update
#          apt-cache search php5

#          which php
#          sudo apt-get install php5
#          sudo ls -l /etc/init.d
